<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mrzeinaliarya3624_mycar.mycarapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ico_car"
        android:label="ماشین من"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MyCarMainActivity"
            android:label="ماشین من"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity_Introduction"
            android:label="@string/title_activity_main__introduction"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>